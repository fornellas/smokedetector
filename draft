Raw input data parser
  Data type definition
    Event separator (\n, ^[0-9]{4}, etc)
    Date extraction regex
    Support multiple event types per input stream
    Event Type
        Field extraction regex
  Event filtering
    Date range
    Field regexp
    Field range
Event processor
  Bucket type
    Field values
    Field value interval
    QPS
  Output histogram
